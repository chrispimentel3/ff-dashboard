# Cloud / app runtime — everything the Streamlit app needs to serve.
# Requires Python 3.10+ (nflreadpy constraint). On Streamlit Community Cloud,
# pick Python 3.12 in the app's Advanced settings.
streamlit>=1.36
nflreadpy>=0.1.5
polars>=1.0
pandas>=2.2
pyarrow>=15
numpy>=1.26

# league command center (all free / no-auth data sources)
httpx>=0.27
lxml>=5.0
feedparser>=6.0
python-dotenv>=1.0
markdown>=3.6            # nicer HTML for the emailed digest

# Desktop-only extras (live Yahoo pull, browser scrape) are NOT installed in the
# cloud — they can't run on an unattended host. See requirements-local.txt.
